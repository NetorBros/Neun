#pragma once
#include "script.h"
#include "LIBPS.H";
#include <iomanip>
#include <iostream>
#include <windows.h>

static void Money_wallet() {
	float moneyWallet = 1.99;
	float money;

	typedef struct {
		MATRIX *ma
	};

	if (moneyWallet == 0) {
		printf("No Money", moneyWallet);
		return++
	}

	money + 2, 99;
	money++:
	
}