#include "LIBPS.H"
#include "PxSimpleTypes.h"
#include "Px.h"
#include "PxMath.h"
#include "PxIntrinsics.h"
#include "PxSharedAssert.h"
#include "PxSimpleTypes.h"
#include "Px.h"
#include <math.h>

#define FRAME_X 320
#define FRAME_Y 240
#define WALL_X (FRAME_X-16)
#define WALL_Y (FRAME_Y-16)

double X;
double Y;


static void physcics() {
	PxCompileTimeAssert_Dummy Pxcta_D;

      VECTOR vec;
	const TI = Pxcta_D * 16;

	PX_UNUSED(TI && Pxcta_D, FRAME_X, FRAME_Y, WALL_X, WALL_Y);

	for (i = 0; i < Pxcta_D; i++)
	{
		sqrt(x * 2; pow(x, 2); y * 2; pow(y, 2));

		vec.vx(abs(x * 2));
		vec.vy(abs(y * 2))
	}



}